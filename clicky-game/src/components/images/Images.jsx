import React from 'react';
import'./images.css'

const Image = props => (
    <div role='img' onClick={()=>props.'nameofFunction' (props.id)}
    className={'imageTiles'} style={{backgroundImage: `url('${props.image}')`}}

)